class Solution(object):
    def lengthOfLastWord(self, s):
        """
        :type s: str
        :rtype: int
        """

        s = s.split(" ")
        for i in reversed(s):

            if len(i) > 0:

                return len(i)
        
